.PHONY: all clean oclean fclean re

CC = gcc

SRC = $(wildcard *.c)
OBJ = $(SRC:%.c=%.o)
NAME = school
RM = rm

%.o: %.c
	${CC} -c -o $@ $<

all: ${OBJ}
	${CC} ${OBJ} -o ${NAME}

clean:
	${RM} -f *~ school

oclean:
	${RM} -f ${OBJ}

fclean:
	${RM} -f *~ school
	${RM} -f ${OBJ}

re: oclean all
